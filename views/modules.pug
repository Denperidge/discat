- for (var i=0; i < modules.length; i++)
    a(onclick="addModule(\"" + modules[i].name + "\");" class="module")
        div
            h1 #{modules[i].name}
            span #{modules[i].description}