div
    - for (var i=0; i < keys.length; i++)
      a(href="/addmodule?id=" + document.body.id + "&module=" + keys[i] class="module")
            div
                h1 #{keys[i]}
                span #{modules[keys[i]].description}